algoritmo "Debug"

var
   x,y,w:inteiro

inicio
      // Entendo o modo Debug e BreakPoint
      // Técnica para fazer testes de mesa
      Escreval("X")
      leia(x)
      Escreval("Y")
      leia(y)
      
      w <- x + y
      Escreval(w)

fimalgoritmo
