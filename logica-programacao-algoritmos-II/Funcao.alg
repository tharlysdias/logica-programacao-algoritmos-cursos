algoritmo "Função"
// O procedimento apenas processo algo de acordo com alguns dados de entrada
// Enquanto na função ela processa esses dados e retorna uma resposta
var

funcao somar(a,b:inteiro) : inteiro
inicio
retorne a + b
fimfuncao

funcao testar() : inteiro
inicio
retorne somar(50,50) + 40
fimfuncao

inicio

Escreval(testar())

fimalgoritmo
